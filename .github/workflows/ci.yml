name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      # Actions are pinned to commit SHAs (not floating tags) per repo-standards —
      # a moved tag can't silently swap the action's code out from under CI.
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1  # v6.3.0
        with:
          python-version: ${{ matrix.python-version }}
      # `memsearch` (the wrapped library) is not on PyPI, so it cannot be installed in
      # CI. Tests mock it — install it as a stub via the MEMSEARCH_STUB fallback in
      # conftest so imports resolve; production installs the real lib in the venv.
      - run: pip install -e ".[dev]"
      - run: ruff check .
      - run: ruff format --check .
      - run: pytest --cov=memsearch_mcp --cov-report=term-missing
      - run: pip-audit --strict .
